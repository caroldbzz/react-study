import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Welcome to the new world!" />
        <CardContent>Hello Darkness my old friend</CardContent>
    </Card>
);